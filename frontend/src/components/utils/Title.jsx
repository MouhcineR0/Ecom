import React from 'react';

function Title({ title }) {
    return (
        <h1 className='text-primary border-l-8 border-l-[#DB4444] px-3 py-1 select-none'>{title}</h1>
    );
}

export default Title;