# version: "3.9"

services:
  backend:
    build: backend
    container_name: backendcontainer
    ports:
      - "3324:3320"
    env_file:
      - backend/.env
  frontend:
    build: frontend
    container_name: frontcontainer
    ports:
      - "3325:5173"
    env_file:
      - frontend/.env
